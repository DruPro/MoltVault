# Voltex — environment configuration
# Copy this to .env and edit as needed.
# docker compose automatically reads .env from the project root.

# Port the dashboard is available on your host machine
FRONTEND_PORT=8080

# Port the vault TCP API is exposed on your host machine
# (used by voltex_client.py when running outside Docker)
API_PORT=7474
